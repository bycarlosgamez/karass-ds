import {
  Meta,
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
} from '@storybook/addon-docs/blocks';
import { Button } from './Button.tsx';
import ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

<Title>Button</Title>

Versatile action triggers for forms, flows, and feedback

## 🎤 Description

Buttons initiate user actions and help guide navigation across interfaces.  
This component supports multiple variants—<code>primary</code>, <code>success</code>, <code>warning</code>, and <code>neutral</code>—tailored to context and intent.

Accessible by default, flexible in layout, and compatible with form submission and asynchronous flows.

## 🚀 Usage

<Primary />
<Controls />

## 🎨 Variations

Explore additional styles and states supported by the Button component.

<Stories />
